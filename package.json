{
  "name": "wordle-247",
  "version": "1.0.0",
  "private": true,
  "description": "Browser based Wordle game with automated unit tests.",
  "type": "module",
  "scripts": {
    "test": "vitest",
    "test:run": "vitest run",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^5.0.1",
    "vitest": "^5.0.1"
  }
}
